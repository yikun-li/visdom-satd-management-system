import {FC} from "react";
import {RestProject} from "../../../types/project";

interface ProjectDashboardProps {
  project: RestProject;
}

export const ProjectDashboard: FC<ProjectDashboardProps> = ({project}) => {
  return <></>;
};
